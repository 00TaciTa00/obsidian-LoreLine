{
  "name": "loreline",
  "version": "0.1.0",
  "description": "Read your markdown notes as a narrative timeline — by time, by place, by character.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && vitest run",
    "deploy": "npm run check && npm run build && node scripts/deploy.mjs",
    "version": "node scripts/version-bump.mjs && git add manifest.json versions.json",
    "upstream": "node scripts/upstream.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "license": "MIT",
  "author": "00tacita00"
}
