# 원본 추적 원장

이 플러그인은 LoreLine 웹앱([00TaciTa00/LoreLine](https://github.com/00TaciTa00/LoreLine))을
옵시디언용으로 옮긴 것이다. 원본에 기능·디자인 변경이 들어오면 여기서 하나씩
검토해 반영 여부를 정하고 기록한다.

검토 완료: `33cb891`

위 커밋까지는 모두 검토했다(아래 표). 새 커밋은 `npm run upstream`으로 본다.

## 절차

1. `npm run upstream` — "검토 완료" 뒤의 커밋을 반영/참고/무시로 가려 보여 준다
2. **반영** 커밋은 원본 diff를 읽고 결정한다
   - 옮긴다 → 플러그인에 구현, 테스트, `npm run check`, 커밋 메시지에 원본 커밋을 적는다
   - 안 옮긴다 → 이유를 표에 적는다 (읽기 전용이라 해당 없음, 옵시디언에 이미 있음 등)
3. 표에 결정을 적고 "검토 완료"를 원본 `origin/main`으로 올린다
4. 뷰·CSS를 건드렸으면 `npm run deploy` 후 옵시디언에서 눈으로 확인한다

**옮길 때의 기준**: 원본의 _의도_(무엇을 왜 바꿨나)를 옮긴다. React·Tailwind 코드를
그대로 옮기지 않는다. 플러그인 쪽 결정(읽기 전용, 이름 매칭, 마크다운 본문,
옵시디언 테마 변수)과 부딪치면 플러그인 쪽을 지키고 표에 적는다.

## 시작점

플러그인은 2026-09-04에 원본 `d47f7c9`(2026-08-24) 시점을 옮겨 시작했다.

그 시점에 원본에 이미 있었지만 옮기지 않은 것:

| 원본 기능 | 상태 | 비고 |
|---|---|---|
| 격자 카드의 딸린 항목 칩 (공간별이면 인물 칩, 인물별이면 공간 칩) + `+N` 접기 | **대기** | 플러그인 카드는 제목·설명만 있다. `92e3654`의 전제 |
| 열 필터 드롭다운 (`LaneFilter`) | 대체 | 플러그인은 칩 줄로 끄고 켠다 |
| 끌어다 놓기 정렬 (`reorder`, `grid-reorder`) | 해당 없음 | 읽기 전용 |

## 검토한 커밋 (`d47f7c9` 이후)

| 원본 | 날짜 | 내용 | 결정 | 비고 |
|---|---|---|---|---|
| `560b2eb` | 09-07 | 시대 페이지 검색 | 해당 없음 | 편집 페이지 |
| `c6a8456` | 09-07 | 사건별 색 삭제, 카드 좌·상단 색 띠 | 반영됨 | 격자: 좌=기간 색, 상=열 색 / 목록: 좌=기간 색. 사건 frontmatter의 `color`도 뺐다(남아 있으면 무시) |
| `c6fe368` | 09-10 | 필터 드롭다운이 sticky 머릿행에 가려짐 | 해당 없음 | 플러그인 필터는 드롭다운이 아니다 |
| `12fe704` | 09-10 | README 사실 오류 | 해당 없음 | 원본 문서 |
| `92e3654` | 09-24 | 격자 카드 칩 2개, 접기 버튼 `-N` | **대기** | 칩 기능(시작점 표) 다음에 |
| `ef88229` `3213c19` `91f9b42` `3ea8e77` | 09-24 | Prettier, .gitignore | 해당 없음 | 원본 도구 |
| `0ee90cf` `9bf8b75` | 09-24 | CI | 해당 없음 | 원본 도구 |
| `87d6b87` `0e996a7` `f83d061` `16b14f4` `a33e9d3` `bfa863a` `0b6bc12` | 09-24 | API 라우트 정리·검증·테스트 | 해당 없음 | 서버 |
| `01b5046` | 09-24 | 격자 가로 배치 순수 로직 (`grid-layout.ts`) | **대기** | 아래 셋은 한 묶음. `event.id`가 문자열(경로)이라 타입만 맞추면 된다 |
| `432028c` | 09-24 | 인물 흐름선 + 사건 카드 가로 병합 | **대기** | 플러그인 격자는 `<table>`이라 CSS grid로 다시 짜야 한다 |
| `0029910` | 09-24 | 흐름선은 인물별 격자에만 | **대기** | `432028c`와 함께 |
| `33cb891` | 09-24 | 격자 시각을 기간 / 시각 두 줄로 | 반영됨 (방식 다름) | 플러그인이 09-04에 먼저 했다(`dc50781`). 원본은 한 문자열에 개행, 플러그인은 `eraName`·`time`을 나눠 기간을 링크로 만든다 |

## 대기 목록 (권장 순서)

1. ~~색 띠~~ (`c6a8456`) — 반영됨
2. **딸린 항목 칩 + 접기** (시작점 격차 + `92e3654`)
3. **가로 병합 + 흐름선** (`01b5046` `432028c` `0029910`) — 격자 구조를 바꾸는 큰 작업
